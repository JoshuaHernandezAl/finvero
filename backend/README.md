# FinVero Backend

## Getting Started

Run mongo with docker compose

```bash
docker-compose up -d
```

Install dependencies

```bash
poetry install
```

Run the application

```bash
poetry run uvicorn main:app --reload
```
